/**
 * Application Route Constants
 * Centralized route paths to avoid magic strings
 */

export const ROUTES = {
    HOME: '/',
    LOGIN: '/login',
    PROFILE: '/profile',
    SETTINGS: '/settings',
} as const;
